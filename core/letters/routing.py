# from django.urls import path

# from .consumers import LetterConsumer

# websocket_urlpatterns = [
#     # path("ws/letters/", LetterConsumer.as_asgi()),
#     path("ws/api/letters/<slug:reference_number>/", LetterConsumer.as_asgi(), name="letter-detail"),
# ]
